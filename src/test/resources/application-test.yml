spring:
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:products;MODE=MYSQL
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop
  sql:
    init:
      mode: always